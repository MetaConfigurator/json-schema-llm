The schema describes a Target object. It is an object type. It defines several reusable components under 'definitions': 'Target' is an object, representing an HTTP request target. It requires the fields 'method' and 'url'. The 'body' property is an optional string, which supports binary encoding in base64. The 'header' property is an optional object, where each property is an array of strings, following a string pattern. The 'method' and 'url' properties are required strings. No additional properties are allowed at the root level.