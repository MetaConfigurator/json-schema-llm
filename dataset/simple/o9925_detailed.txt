The schema describes an object structure. It is an object type. The main properties of the schema are: 'attribute' is a string that must match the pattern '^[0-9a-zA-Z_-]{1,255}$' and is optional. 'deviceTags' is an array of objects where each object must have a 'key' that is a string matching the pattern '^[0-9a-zA-Z_-]{1,255}$' and a 'value' that is a string with a minimum length of 1 and a maximum length of 255. Each object in 'deviceTags' cannot have additional properties, and the array can contain up to 100 items. 'deviceIds' is an array of strings, each matching the pattern '^[A-Fa-f\d]{24}$', with a maximum of 1000 items. No additional properties are allowed at the root level.