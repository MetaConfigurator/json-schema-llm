The schema describes the data contained in a LineChart Stream update object. It is an object type. The main properties of the schema are: 'stream' is an array that contains objects. Each object is required to have 'label' as a string and 'data' as an array of numbers, both of which are required. No additional properties are allowed at the root level.