The schema describes an object structure. It is an object type. The main properties of the schema are: 'name' is a string with a minimum length of 1 and a maximum length of 255, and it is required. 'expirationDate' is a string formatted as 'date-time' and is optional. 'scope' is an array of strings and is optional. 'status' is a string with an enum that accepts either 'active' or 'inactive', and it is optional. No additional properties are allowed at the root level.