The schema describes the format of requests to Celium nodes. It is an object type. The main properties of the schema are: 'commands' is an array of commands, required, containing at least one item. Each item in the array is an object with properties: 'name', which is a string, required, and 'params', which is an object. No additional properties are allowed at the root level.