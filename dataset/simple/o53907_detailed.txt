The schema describes 10/EPMP get metadata request. It is an object type. The main properties of the schema are: 'keys' is an array of strings representing endpoint metadata key names. It is optional and must match the pattern '^[a-zA-Z0-9_]+$'. The items in the array must be unique. If not specified, all EP metadata keys will be returned. No additional properties are allowed at the root level.