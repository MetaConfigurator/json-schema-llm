The schema describes an array of game base variants. It is an array type. It defines several reusable components under 'definitions': 'GameBaseVariant' is an object, which has required fields: 'name', 'internalName', 'iconUrl', 'supportedGameModes', 'id', and 'contentId'. The 'name' field is a nullable string. The 'internalName' field is a string. The 'iconUrl' field is a nullable string. The 'supportedGameModes' field is a nullable array, which can only contain strings and does not allow additional items. The 'id' field is a string. The 'contentId' field is a string. The main properties of the schema are: No additional properties are allowed at the root level.