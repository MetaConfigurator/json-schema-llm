The schema describes an array of objects. It is an array type. The main properties of the schema are: Each object in the array must have 'name' as a string, 'rank' as a string, and 'joined' as a string. No additional properties are allowed at the root level.