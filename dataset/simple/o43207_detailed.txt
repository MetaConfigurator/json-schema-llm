The schema describes an array of maps. It is an array type. It defines several reusable components under 'definitions': 'Map' is an object, representing a map with several required fields. It includes 'name' which can be a string or null, 'description' which can also be a string or null, 'supportedGameModes' which is an array of strings, and 'imageUrl', 'id', and 'contentId', all of which are strings. The 'name', 'description', 'supportedGameModes', 'imageUrl', 'id', and 'contentId' fields are required. No additional properties are allowed at the root level.