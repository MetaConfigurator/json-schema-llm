The schema describes an array of objects representing items with associated facts and attributes. It is an array type. The main properties of the schema are: Each object must include 'name', 'facts', 'description', 'icon', 'flags', 'id', and 'chat_link', which are all required fields. The 'facts' property is an array of objects, where each object requires 'text', 'type', and 'icon' as required fields, and may also contain optional fields like 'value', 'hit_count', and 'dmg_multiplier'. No additional properties are allowed at the root level.