The schema describes a structured object with pattern-based properties. It is an object type. It defines several reusable components under 'definitions': 'inexclude' is an object type, which requires the fields 'combined' (a boolean), 'path' (an argument type), and 'file' (also an argument type). 'argument' is a type that can be either an object or null, requiring the field 'parameter' (a string) and optionally containing fields 'separator' (a string or null), 'multiple' (a boolean), and 'relative' (a boolean). The main properties of the schema are: The pattern for properties requires keys matching the regex '^[a-z0-9_-]+/[a-z0-9_-]+$' which map to an object type containing properties with keys matching the regex '^[0-9.*]+(,[0-9.*]+)*$'. These objects require the fields 'exclude' (an inexclude type) and 'include' (also an inexclude type) and can contain an array named 'order'. No additional properties are allowed at the root level.