The schema describes an array of objects. It is an array type. The main properties of the schema are: Each object contains 'id', which is a string and is required; 'deaths', which is an object and is required, containing 'red', 'blue', and 'green', all of which are numbers; 'kills', which is an object and is required, containing 'red', 'blue', and 'green', all of which are numbers; and 'maps', which is an array of objects and is required. Each object in 'maps' must have 'id', which is a number, 'type', which is a string, 'deaths', which is an object containing 'red', 'blue', and 'green', all of which are numbers, and 'kills', which is an object containing 'red', 'blue', and 'green', all of which are numbers. No additional properties are allowed at the root level.