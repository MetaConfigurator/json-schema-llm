The schema describes a structured event message. It is an object type. The main properties of the schema are: 'meta' is an object containing metadata about the event, which is required. It has required fields: 'id', which is a string matching the pattern '^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$', 'type', which is a string with an enum value of 'EiffelFlowContextDefinedEvent', 'version', which is a string with an enum value of '2.0.0' and a default of '2.0.0', and 'time', which is an integer. The 'tags' property is an optional array of strings. The 'source' property is an object with optional fields 'domainId', 'host', 'name', 'serializer', and 'uri', and does not allow additional properties. The 'security' property is an object that contains a required 'sdm' object, which has required fields 'authorIdentity' and 'encryptedDigest', and does not allow additional properties. 'data' is an object containing optional fields: 'product', 'project', 'program', 'track', 'version', and 'customData', which is an optional array of objects each containing required fields 'key' (a string) and 'value' (of any type) and does not allow additional properties. 'links' is an array of objects, each containing required fields: 'type', which is a string, and 'target', which is a string matching the pattern '^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$', and does not allow additional properties. No additional properties are allowed at the root level.