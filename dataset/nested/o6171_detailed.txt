The schema describes an object structure. It is an object type. It defines several reusable components under 'definitions': 'status' is a string, which can be either 'active' or 'inactive'. The main properties of the schema are: 'id' is a string matching the pattern '^[A-Fa-f\d]{24}$', which is optional. 'applicationKeyId' is a string matching the pattern '^[A-Fa-f\d]{24}$', which is optional. 'applicationId' is a string matching the pattern '^[A-Fa-f\d]{24}$', which is optional. 'creationDate' is a string in date-time format, which is optional. 'lastUpdated' is a string in date-time format, which is optional. 'key' is a string, which is optional. 'status' is a string that references the definition 'status', which is optional. 'secret' is a string, which is optional. 'deviceIds' is an array of strings, where each string must match the pattern '^[A-Fa-f\d]{24}$', which is optional. 'deviceTags' is an array of objects, where each object has properties 'key' and 'value', both of which are strings. No additional properties are allowed at the root level.