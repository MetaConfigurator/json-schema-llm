The schema describes an object structure. It is an object type. The main properties of the schema are: 'adm' is an array of objects, required, where each object must have 'id' (a string) and 'data' (an object). The 'data' object may contain 'img' (a string), 'text' (a string), and 'app_url' (a string). The 'adm' objects may optionally include 'click_through_url' (an array of strings) and 'tracking_url' (an array of strings). 'is_test' is an integer, required, with allowed values 0 and 1. No additional properties are allowed at the root level.