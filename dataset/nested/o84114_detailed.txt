The schema describes the Kraken API. It is an object type. It defines several reusable components under 'definitions': 'fileservertype' is a string type, representing the type of file server. 'mount' is an object type, which contains properties: 'id' as a reference to a string type definition, 'source' as a reference to a string type definition, and 'target' as a reference to a string type definition. 'server' is an object type, which contains properties: 'bind_address' as a reference to a string type definition, 'mounts' as an array of 'mount' definitions, and 'port' as an integer type definition. The main properties of the schema are: 'file-server-type' is a string type, referencing the 'fileservertype' definition, 'mount' is an object type, referencing the 'mount' definition, and 'server' is an object type, referencing the 'server' definition. No additional properties are allowed at the root level.