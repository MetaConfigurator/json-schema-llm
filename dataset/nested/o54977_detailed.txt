The schema describes an object representing a bidding request. It is an object type. The main properties of the schema are: 'id' is a string, optional; 'createDate' is an integer, optional; 'updateDate' is an integer, optional; 'requester' is an object containing 'id' as a string (optional), 'login' as a string (optional), 'firstName' as a string (optional), and 'lastName' as a string (optional); 'responder' is an object containing 'id' as a string (optional), 'login' as a string (optional), 'firstName' as a string (optional), and 'lastName' as a string (optional); 'latestBid' is an object containing 'placeDate' as an integer (optional), 'bidder' as an object with 'id' as an integer (optional), 'login' as a string (optional), 'firstName' as a string (optional), and 'lastName' as a string (optional), 'offeredItems' as an array of objects with 'id' as a string (optional) and 'name' as a string (optional), and 'requestedItems' as an array of objects with 'id' as a string (optional) and 'name' as a string (optional). No additional properties are allowed at the root level.