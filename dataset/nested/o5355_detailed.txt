The schema describes a structured event containing metadata, data, and links. It is an object type. The main properties of the schema are: 'meta' is an object containing metadata about the event. It is required and cannot have additional properties. It includes 'id', which is a string that must match the specified UUID pattern; 'type', which is a string with an enum value of 'EiffelArtifactReusedEvent'; 'version', which is a string with an enum value of '1.1.0' and has a default value of '1.1.0'; and 'time', which is an integer that records the timestamp. The 'data' property is an object containing additional information related to the event. It is required and cannot have additional properties, including 'customData', which is an array of objects. Each object in the array must contain a 'key' as a string and a 'value' of any type, both of which are required. The 'links' property is an array of objects. Each object must contain 'type' as a string and 'target' as a string that matches the UUID pattern, both of which are required. No additional properties are allowed at the root level.