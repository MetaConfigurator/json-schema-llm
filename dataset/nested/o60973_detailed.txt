The schema describes the result of OSCryptoChecker worker. It is an object type. It defines several reusable components under 'definitions': "cryptoalgo_detail" is an object, which requires the fields "crypto", "file", and "matchtype". The fields "matchtype" can be either "content" or "filename", and additionalProperties are not allowed. "cryptoalgo_record" is an object, which requires the fields "count" and "name", and additionalProperties are not allowed. "cryptocheck_summary" is an object, which requires the fields "content" and "filename", which are arrays containing items that reference the "cryptoalgo_record" definition, and additionalProperties are not allowed. The main properties of the schema are: "schema" is an object that requires the fields "name" (matching the pattern "^[a-zA-Z0-9_]+$") and "version" (matching the pattern "^[0-9]+-[0-9]+-[0-9]+$"), with no additionalProperties allowed; "_release" is an optional string, describing a unique release id in the format of "ecosystem:package:version"; "status" is an optional string, which can either be "success" or "error"; "details" is an optional array with items referencing the "cryptoalgo_detail" definition; "summary" is an object that references the "cryptocheck_summary" definition. No additional properties are allowed at the root level.