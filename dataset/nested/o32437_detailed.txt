The schema describes a single plurality race. It is an object type. The main properties of the schema are: 'isTest' is a boolean value that is always true if it exists, could later be used to specify some useful info about the test. It is optional and has an enum with the allowed value of true. 'counts' is an array that specifies how many of each of the ballots in votes will appear, and can be used for a compressed form of tests. It is optional and contains integers as items. 'votes' is an array of objects, each representing all of the votes in the election. Each object must have the properties 'id' and 'selection', both of which are required integers. No additional properties are allowed at the root level. 'tiebreak' is an integer representing the candidate that should be chosen in the event of a tie, and is optional. 'results' is an object that records what happened as the election was tabulated; it is required. This object must have the property 'winner', which is a required integer indicating which candidate won the election. The 'tabulation' property is an array of objects that require 'candidate' and 'votes' as integers, and also must not allow additional properties. The schema has a dependency that states if 'isTest' is present, then 'results', 'tiebreak', and 'counts' must also be present. No additional properties are allowed at the root level.