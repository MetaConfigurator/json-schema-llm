The schema describes a response containing traffic flows between objects. It is an object type. The main properties of the schema are: 'header' is an object that describes document data, which is optional. It contains properties such as 'time_range', an object that describes the time range of the document records, which is optional and includes 'start' and 'end', both strings that represent ISO8601 timestamps of the earliest and latest records respectively. Another property is 'recordCount', which is an integer that represents the total record count, and 'objTypes', which is an array of strings that represents a collection of object types. 'data' is an array that contains historic records and is required. Each item in the array is an object that has properties: 'srcObj', a string representing the name/ID of the source object; 'srcType', a string for the source object's type; 'destObj', a string for the name/ID of the destination object; 'destType', a string for the destination object's type; 'bytes', an integer representing the volume of data flowing from source to destination, with a minimum of 0; and 'packets', an integer representing the number of packets sent from source to destination, with a minimum of 0. No additional properties are allowed at the root level.