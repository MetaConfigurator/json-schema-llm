The schema describes a user registration object. It is an object type. The main properties of the schema are: 'email' is a string that must follow the email format and has a maximum length of 1024 characters, and it is required. 'experienceGroupIds' is an array of strings that can include up to 1000 items, each matching the pattern '^[A-Fa-f\d]{24}$'. 'firstName' is a string with a maximum length of 1024 characters and is optional. 'lastName' is a string with a maximum length of 1024 characters and is optional. 'password' is a string that has a minimum length of 8 characters, a maximum length of 2048 characters, and it is required. 'tokenCutoff' is a string that must follow the date-time format and is optional. 'userTags' is an object that does not allow additional properties, where property names must match the pattern '^[0-9a-zA-Z_-]{1,255}$', and each property value must be a string with a length between 1 and 255 characters. No additional properties are allowed at the root level.