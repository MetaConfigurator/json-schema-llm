The schema describes a molecular structure configuration. It is an object type. It defines several reusable components under 'definitions': The main properties of the schema are: 'version' is an object, optional, with no further specifications. 'notes' is a string, required. 'include' is an array of strings, optional, ensuring unique items. 'molecules' is an object, required, allowing additional properties where each property is an object with specified properties. These properties include: 'polymer_type' which is an array of strings, ensuring unique items, 'comment' which is a string, optional, 'atoms' which is an array of objects, ensuring unique items, where each object is required to have 'name' and 'type' as strings; 'lengths' which is an array of objects, ensuring unique items, where each object has a 'type' string and an 'atoms' array of strings with exactly 2 unique items; 'angles' which is an array of objects, ensuring unique items, where each object has a 'type' string and an 'atoms' array of strings with exactly 3 unique items; 'dihedrals' which is an array of objects, ensuring unique items, where each object has a 'type' string and an 'atoms' array of strings with exactly 4 unique items; 'impropers' which is an array of objects, ensuring unique items, where each object has a 'type' string and an 'atoms' array of strings with exactly 4 unique items. No additional properties are allowed at the root level.