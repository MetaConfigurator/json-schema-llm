The schema describes a set of gateways. It is an array type. The main properties of the schema are: [items] is an object that represents a gateway. It has required properties: 'apiVersion', 'kind', 'metadata', and 'spec'. 'apiVersion' is an enum, allowing values 'v1beta1', 'v1.0', 'v1'. 'kind' is an enum, allowing values 'Gateway' and 'gateway', and it is read-only. 'metadata' is an object that requires 'name', which is a string with a minimum length of 3 and a maximum length of 30. It also contains 'createdOn', 'modifiedOn', and 'ref', which are strings that are read-only. 'spec' is an object that requires 'host' (a string with a minimum length of 1) and 'transport' (an enum allowing values 'tcp' and 'udp'). It also includes optional properties: 'credentials' (an object requiring 'username' which is a string with a minimum length of 3 and a maximum length of 30, and 'secret' which is write-only), 'expires' (an integer with a minimum of 0), 'port' (an integer), and 'registries' (an array of strings with a minimum of 1 item and unique items). No additional properties are allowed at the root level.