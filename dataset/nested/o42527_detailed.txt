The schema describes the cloud context information. It is an object type. The main properties of the schema are: '_schema' is a string that follows the pattern '^pbj:([a-z0-9-]+):([a-z0-9\.-]+):([a-z0-9-]+)?:([a-z0-9-]+):([0-9]+-[0-9]+-[0-9]+)$' and is required, with a default value of 'pbj:gdbots:contexts::cloud:1-0-0'. 'provider' is a string that follows the pattern '^([\w\/-]|[\w-][\w\/-]*[\w-])$', with a minimum length of 0, a maximum length of 20, and is optional. 'region' is a string that follows the same pattern as 'provider', with identical length constraints, and is optional. 'zone' is a string that follows the same pattern as 'provider', with identical length constraints, and is optional. 'instance_id' is a string that follows the pattern '^[\w\.-]+$' and is optional. 'instance_type' is a string that follows the same pattern as 'instance_id' and is optional. No additional properties are allowed at the root level.