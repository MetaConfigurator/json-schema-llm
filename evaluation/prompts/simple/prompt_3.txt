The schema describes JSON schema for Import Maps files. It is an object type. The main properties of the schema are: 'imports' is an object that represents the Imports field, which allows additional properties of type string. 'scopes' is an object that represents the Scopes field, which allows additional properties that are also objects, and each of those may have additional properties of type string. No additional properties are allowed at the root level.