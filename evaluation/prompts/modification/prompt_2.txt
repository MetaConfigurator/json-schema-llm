Modify the following schema:

{
    "$schema": "http://json-schema.org/draft-04/schema",
    "description": "Trait that applies common publishing-related dates to a schema.",
    "properties": {
        "created_date": {
            "_format": "date-time",
            "description": "When the content was originally created (RFC3339-formatted).",
            "type": "string"
        },
        "last_updated_date": {
            "_format": "date-time",
            "description": "When the content was last updated (RFC3339-formatted).",
            "type": "string"
        }
    },
    "title": "Dated trait",
    "type": "object"
}

Make the following changes:
- Add a required property num_updates. num_updates is an integer value which shouldn't be greater than 50.
